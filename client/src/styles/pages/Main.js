import styled from 'styled-components'

const MainContainer = styled.div`
    display: flex;
    .routes{
        flex: 1;
        padding-left: 75px;
        background: var(--grey);
        min-height: 100vh;
        height: 100%;
    }
`

export default MainContainer;